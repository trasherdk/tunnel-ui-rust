[package]
name = "tunnel-ui"
version = "0.1.0"
edition = "2021"
description = "Terminal UI for SSH local port forwards (Linux)"
publish = false

[dependencies]
anyhow = "1"
libc = "0.2"
ratatui = "0.29"
regex = "1"

[dev-dependencies]
tempfile = "3"
